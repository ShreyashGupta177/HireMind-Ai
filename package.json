{
  "name": "hiremind-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:ml\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm start",
    "dev:ml": "cd backend && source venv/bin/activate && python ml_service.py",
    "dev:ml-win": "cd backend && venv\\Scripts\\activate && python ml_service.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}